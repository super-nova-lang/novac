open std.c with { printf }

let show :: label: string, value: i32 = {
    printf("%s = %d\n", label, value)
}

let main :: () = {
    show(~value = 42, ~label = "answer")
    show("positional", ~value = 7)
}
