open std.c with { printf }

let main :: () = {
    let x := 10
    printf("x before: %d\n", x)
    x -= 3
    printf("x after: %d\n", x)
    x += 3
    printf("x after: %d\n", x)
    x *= 2
    printf("x after: %d\n", x)
    x /= 10
    printf("x after: %d\n", x)
}
