open std.c with { printf }

let add7 :: a: i32, b: i32, c: i32, d: i32, e: i32, f: i32, g: i32 -> i32 = {
    a + b + c + d + e + f + g
}

let main :: () = {
    let result = add7(1, 2, 3, 4, 5, 6, 7)
    printf("sum=%d\n", result)
}
