open std.c with { stdio.printf }

let countdown :: n = {
    while n > 0 {
        printf("n = %d\n", n)
        let n = n - 1
    }
    printf("done!\n")
}

let main :: () = {
    countdown(3)
    ()
}