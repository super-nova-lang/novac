open std.c with { stdio.printf }

let main :: () = {
    let flag = 0
    if flag {
        let tmp_then = printf("then branch\n")
    } else {
        let tmp_else = printf("else branch\n")
    }
    ()
}
