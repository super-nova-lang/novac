module Math {
  export add
  export { sub as subtract }

  let add :: (a: i32, b: i32) -> i32 = {
    a + b
  }

  let sub :: (a: i32, b: i32) -> i32 = {
    a - b
  }
}

let main :: () = {
  let x = Math.add(10, 5);
  let y = Math.sub(10, 5);
  x + y
}
